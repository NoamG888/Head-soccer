class Text:

    def __init__(self, text, x_pos, y_pos, size, color, font):
        self.text = text
        self.x_pos = x_pos
        self.y_pos = y_pos
        self.size = size
        self.color = color
        self.font = font
